<?xml version="1.0" encoding="utf-8"?>
<!-- shape的默认形状是rectangle，oval(椭圆),line（线）,ring（圆环）-->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置背景填充色 -->
    <solid android:color="@color/colorWhite" />
    <!-- 设置边框宽度为1dp，边框颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/colorGrey" />
</shape>
